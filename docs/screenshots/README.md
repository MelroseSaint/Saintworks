# Screenshots

Drop captured screenshots here and they will appear in the root `README.md`.

Recommended captures:

- `editor.png` — the full IDE (toolbar, explorer, canvas, inspector)
- `canvas.png` — the visual canvas with an element selected
- `brand.png` — the brand builder / tokens view
- `timeline.png` — the timeline animation editor
- `publish.png` — the publishing workflow

Keep captures at 1x–2x resolution and reference them from `README.md` as:

```markdown
![SaintWorks IDE](docs/screenshots/editor.png)
```
